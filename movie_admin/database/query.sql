CREATE TABLE `admin` (
    `id` INT PRIMARY KEY AUTO_INCREMENT NOT NULL,
    `username` VARCHAR(255),
    `password` VARCHAR(255)
);

INSERT INTO `admin` (`username`, `password`)
VALUES ('soroMalick', 'onelove123');